// Verbatim donut exposure pass — see RenderingPlugin/External/donut/shaders/passes/exposure_cs.hlsl.
// Kept identical to the original by including it directly; compile settings (HISTOGRAM_BINS, include
// paths, cs_6_6, entry `main`) live in the .meta importer.
#include "passes/exposure_cs.hlsl"
